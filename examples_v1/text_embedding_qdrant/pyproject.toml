[project]
name = "text-embedding-qdrant-v1"
version = "0.1.0"
description = "CocoIndex v1 example: embed markdown files and store in Qdrant."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[embeddings,qdrant]>=0.3.9",
    "numpy",
    "sentence-transformers>=3.0.0",
    "qdrant-client>=1.6.0",
]

[tool.setuptools]
packages = []
