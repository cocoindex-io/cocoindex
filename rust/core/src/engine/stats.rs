pub struct ProcessingStatsGroup {
    pub num_starts: u64,
    pub num_ends: u64,

    pub num_adds: u64,
    pub num_deletes: u64,
    pub num_updates: u64,
    pub num_errors: u64,
}

pub struct ProcessingStats {}
