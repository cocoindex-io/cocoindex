# CocoIndex core (PostgreSQL for internal state)
# Note: Using port 5433 to avoid conflicts with local PostgreSQL
COCOINDEX_DATABASE_URL=postgres://cocoindex:cocoindex@localhost:5433/cocoindex

# Fallback to CPU for operations not supported by MPS on Mac.
# It's no-op for other platforms.
PYTORCH_ENABLE_MPS_FALLBACK=1

# Doris/VeloDB connection configuration
# Required:
DORIS_FE_HOST=localhost
DORIS_PASSWORD=

# Set this when running Doris in Docker on macOS/Windows.
# Stream Load redirects from FE to BE use Docker-internal IPs that are
# unreachable from the host. This rewrites the redirect to localhost.
DORIS_BE_LOAD_HOST=localhost

# Optional (defaults shown):
# DORIS_USERNAME=root
# DORIS_HTTP_PORT=8030
# DORIS_QUERY_PORT=9030
# DORIS_DATABASE=sec_analytics

# For VeloDB Cloud:
# DORIS_FE_HOST=your-cluster.velodb.cloud
# DORIS_PASSWORD=your-password
# DORIS_USERNAME=admin
# DORIS_HTTP_PORT=443
# DORIS_QUERY_PORT=9030
# DORIS_DATABASE=sec_analytics

# Optional: LLM summary (Anthropic Claude)
# ANTHROPIC_API_KEY=sk-ant-...
