# postgres_source — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# Postgres + pgvector target. Start a local one: docker compose -f ../../dev/postgres.yaml up -d
POSTGRES_URL=postgres://cocoindex:cocoindex@localhost/cocoindex

# Source Postgres to read product rows from (can be the same instance).
SOURCE_DATABASE_URL=postgres://cocoindex:cocoindex@localhost/cocoindex
