# code_embedding_lancedb — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# LanceDB is an embedded, local store — no server needed.
# Optional: point at a different LanceDB directory (default: ./lancedb).
# LANCEDB_URI=./lancedb
