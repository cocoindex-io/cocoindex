[project]
name = "text-embedding-chromadb"
version = "0.1.0"
description = "Simple example for cocoindex: build embedding index based on local text files with ChromaDB."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[embeddings,chromadb]>=0.3.26,<1",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
