[project]
name = "text-embedding-v1"
version = "0.1.0"
description = "CocoIndex v1 example: embed local markdown files and store in Postgres/pgvector."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[postgres,sentence_transformers]>=0.3.9",
    "asyncpg>=0.29.0",
    "pgvector>=0.4.1",
    "numpy",
]

[tool.setuptools]
packages = []
