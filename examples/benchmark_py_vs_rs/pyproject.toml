[project]
name = "benchmark_py_vs_rs"
version = "0.1.0"
description = "Benchmarking cocoindex python vs rust"
requires-python = ">=3.11"
dependencies = [
    "cocoindex",
]

[tool.uv.sources]
cocoindex = {path = "../../", editable = true}
