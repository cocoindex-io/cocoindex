# text_embedding_qdrant — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# Qdrant vector store (default is a local container).
# Start one: docker run -d -p 6333:6333 -p 6334:6334 qdrant/qdrant
# Optional override (default: http://localhost:6334/):
# QDRANT_URL=http://localhost:6334/
