[project]
name = "text-embedding-turbopuffer-v1"
version = "0.1.0"
description = "CocoIndex v1 example: embed markdown files and store in Turbopuffer."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[sentence_transformers,turbopuffer]>=1.0.2",
    "numpy",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
