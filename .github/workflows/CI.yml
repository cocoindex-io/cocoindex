name: CI

on:
  pull_request:
    branches: [main, v1]
    paths:
      - rust/**
      - python/**
      - "*.toml"
      - ".github/workflows/*.yml"
      - "docs/docs/cli_commands.md"
  push:
    branches: [main, v1]
    paths:
      - rust/**
      - python/**
      - "*.toml"
      - ".github/workflows/*.yml"
      - "docs/docs/cli_commands.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-test:
    name: Build & Test
    uses: ./.github/workflows/_test.yml
