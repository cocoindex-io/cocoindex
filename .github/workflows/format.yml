# This file is autogenerated by maturin v1.8.1
# To update, run
#
#    maturin generate-ci github
#
name: Format Check

on:
  pull_request:
    branches: [main]
    paths:
      - rust/**
      - python/**
      - examples/**
  push:
    branches: [main]
    paths:
      - rust/**
      - python/**
      - examples/**
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rust-format-check:
    name: Check Rust formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Check Rust formatting
        run: |
          cargo fmt --check

  python-format-check:
    name: Check Python formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Install uv
        uses: astral-sh/setup-uv@v4
      - name: Check Python formatting
        run: uv run --no-dev --group format ruff format --check .